name: Milestone_next-patch

on:
  pull_request_target:
    branches:
      - 'develop'

jobs:
  assign-milestone-patch:
    runs-on: ubuntu-latest

    steps:
      - uses: zoispag/action-assign-milestone@v1
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
          milestone: 'next-patch'